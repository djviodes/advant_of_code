use std::fs::read_to_string;

fn main() {
    println!("Calculating the total priority for the item that appears in both comparments of each rucksack!");

    calculate_priority();
}

fn calculate_priority() {
    
}